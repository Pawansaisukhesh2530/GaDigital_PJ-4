# Large media that exceeds GitHub's 100MB per-file limit.
# The projects hero video must be hosted separately or via Git LFS.
assets/videos/*.mov
assets/videos/*.mp4
assets/videos/*.webm

# OS / editor junk
Thumbs.db
ehthumbs.db
Desktop.ini
desktop.ini
.DS_Store
*.tmp
